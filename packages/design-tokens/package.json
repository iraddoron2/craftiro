{
    "name": "@craftiro/design-tokens",
    "private": true,
    "version": "0.1.0",
    "files": [
        "dist"
    ],
    "scripts": {
        "clean": "rimraf dist || true",
        "build:light": "THEME=light style-dictionary build --config ./style-dictionary.config.cjs",
        "build:dark": "THEME=dark style-dictionary build --config ./style-dictionary.config.cjs",
        "build": "pnpm run build:light && pnpm run build:dark"
    },
    "devDependencies": {
        "style-dictionary": "^3.9.2",
        "rimraf": "^5.0.5"
    }
}
