export type ExerciseAuthor = {
    _id: string // Unique identifier for the author
    name: string // Name of the author
    profilePictureUrl?: string // Optional URL for the author's profile picture
    bio?: string // Optional short biography of the author
}
